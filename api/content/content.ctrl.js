// https://expressjs.com/en/api.html#res

import Joi from "joi";
import UserSchema from "@SCHEMA/ContentSchema";

const _status = {
  401: { status: "Unauthorized" },
};

export const write = async (ctx) => { };
export const modify = async (ctx) => { };
